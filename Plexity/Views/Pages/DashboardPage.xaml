<Page
    x:Class="Plexity.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Plexity.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DashboardPage"
    d:DataContext="{d:DesignInstance local:DashboardPage, IsDesignTimeCreatable=False}"
    d:DesignHeight="800"
    d:DesignWidth="1200"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="WelcomeText"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Text="Welcome, user..." />
            <TextBlock Text="Install mods and copy fast flags quickly and easily."
                       Margin="0,5,0,0"
                       FontSize="12"
                       Foreground="#FF9E9E9E"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10" Width="380" HorizontalAlignment="Left" Height="35">
            <TextBox x:Name="SearchBox"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Padding="10"
                     Background="#222"
                     Foreground="White"
                     BorderThickness="1"
                     BorderBrush="Gray"
                     TextChanged="SearchBox_TextChanged" />

            <TextBlock x:Name="SearchPlaceholder"
                       Text="Search for mods..."
                       Foreground="Gray"
                       Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       FontSize="14"
                       Focusable="False"/>
        </Grid>

        <!-- Mod List Area -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="ModListPanel" Margin="0">
                <Border Width="350" Height="219" CornerRadius="5" Margin="5" Padding="10" BorderThickness="1" BorderBrush="#FF171717">
                    <Border.Background>
                        <ImageBrush 
            ImageSource="/Views/Pages/WOW.png"
            Stretch="UniformToFill"
            RenderOptions.BitmapScalingMode="LowQuality" />
                    </Border.Background>
                    <StackPanel>
                        <TextBlock Text="Maow Editor" FontWeight="Bold" FontSize="16" Foreground="White"/>
                        <TextBlock Text="A nice clean editor for you WOW."
                   FontSize="12" Foreground="#FF9E9E9E" TextWrapping="Wrap" Margin="0,5,0,10"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0">
                            <Button Content="Install" Width="120" Margin="0,115,5,0"/>
                            <Button Content="Open Mods" Width="120" Margin="0,115,0,0" />
                        </StackPanel>
                    </StackPanel>
                </Border>


            </WrapPanel>
        </ScrollViewer>
    </Grid>
</Page>
