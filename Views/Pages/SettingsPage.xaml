<Page x:Class="Plexity.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="SettingsPage"
      d:DataContext="{d:DesignInstance local:SettingsViewModel, IsDesignTimeCreatable=False}"
      d:DesignHeight="450"
      d:DesignWidth="800"
      mc:Ignorable="d">

    <Grid Margin="{StaticResource StandardMargin}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Application Settings -->
                <ui:CardControl Margin="{StaticResource CardMargin}">
                    <ui:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ui:SymbolIcon Symbol="Settings24" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="Application Settings" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </Grid>
                    </ui:CardControl.Header>
                    
                    <StackPanel Margin="{StaticResource GroupBoxPadding}">
                        <!-- Theme Settings -->
                        <TextBlock Text="Theme" Margin="{StaticResource InnerControlMargin}"/>
                        <ComboBox ItemsSource="{Binding ThemeOptions}"
                                  SelectedItem="{Binding CurrentTheme}"
                                  Height="{StaticResource StandardControlHeight}"
                                  Margin="{StaticResource ControlSpacing}"/>
                        
                        <!-- Animation Settings -->
                        <CheckBox Content="Disable Animations"
                                  IsChecked="{Binding DisableAnimations}"
                                  Margin="{StaticResource ControlSpacing}"/>
                        
                        <!-- Debug Logging -->
                        <CheckBox Content="Enable Debug Logging"
                                  IsChecked="{Binding DebugLogging}"
                                  Margin="{StaticResource ControlSpacing}"/>
                                  
                        <!-- Navigation Pane -->
                        <TextBlock Text="Navigation Pane Style" Margin="{StaticResource InnerControlMargin}"/>
                        <ComboBox ItemsSource="{Binding PaneOptions}"
                                  SelectedItem="{Binding CurrentPane}"
                                  Height="{StaticResource StandardControlHeight}"
                                  Margin="{StaticResource ControlSpacing}"/>
                    </StackPanel>
                </ui:CardControl>
                
                <!-- Roblox Settings -->
                <ui:CardControl Margin="{StaticResource CardMargin}">
                    <ui:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ui:SymbolIcon Symbol="Games24" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="Roblox Settings" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </Grid>
                    </ui:CardControl.Header>
                    
                    <StackPanel Margin="{StaticResource GroupBoxPadding}">
                        <!-- Roblox Priority -->
                        <TextBlock Text="Roblox Process Priority" Margin="{StaticResource InnerControlMargin}"/>
                        <ComboBox ItemsSource="{Binding PriorityOptions}"
                                  SelectedItem="{Binding SelectedPriority}"
                                  Height="{StaticResource StandardControlHeight}"
                                  Margin="{StaticResource ControlSpacing}"/>
                        
                        <!-- Keep Open -->
                        <CheckBox Content="Keep Plexity Open When Launching Roblox"
                                  IsChecked="{Binding KeepPlexityOpen}"
                                  Margin="{StaticResource ControlSpacing}"/>
                        
                        <!-- Force Reinstallation -->
                        <CheckBox Content="Force Roblox Reinstallation"
                                  IsChecked="{Binding ForceRobloxReinstallation}"
                                  Margin="{StaticResource ControlSpacing}"/>
                    </StackPanel>
                </ui:CardControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>